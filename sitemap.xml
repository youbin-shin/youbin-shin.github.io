---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9 http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd" xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for post in site.posts %}
    <url>
      <loc>{{ site.url }}{{ post.url }}</loc>
      {% if post.lastmod == null %}
        <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
      {% else %}
        <lastmod>{{ post.lastmod | date_to_xmlschema }}</lastmod>
      {% endif %}

      {% if post.sitemap.changefreq == null %}
        <changefreq>weekly</changefreq>
      {% else %}
        <changefreq>{{ post.sitemap.changefreq }}</changefreq>
      {% endif %}

      {% if post.sitemap.priority == null %}
          <priority>0.5</priority>
      {% else %}
        <priority>{{ post.sitemap.priority }}</priority>
      {% endif %}

    </url>
  {% endfor %}
  {% for tab in site.tabs %}
    <url>
      <loc>{{ site.url }}{{ tab.url }}</loc>
      {% if tab.lastmod == null %}
        <lastmod>{{ tab.date | date_to_xmlschema }}</lastmod>
      {% else %}
        <lastmod>{{ tab.lastmod | date_to_xmlschema }}</lastmod>
      {% endif %}

      {% if tab.sitemap.changefreq == null %}
        <changefreq>weekly</changefreq>
      {% else %}
        <changefreq>{{ tab.sitemap.changefreq }}</changefreq>
      {% endif %}

      {% if tab.sitemap.priority == null %}
          <priority>0.5</priority>
      {% else %}
        <priority>{{ tab.sitemap.priority }}</priority>
      {% endif %}
    </url>
  {% endfor %}
  {% for category in site.categories %}
    <url>
      <loc>{{ site.url }}{{ category.url }}</loc>
      {% if category.lastmod == null %}
        <lastmod>{{ category.date | date_to_xmlschema }}</lastmod>
      {% else %}
        <lastmod>{{ category.lastmod | date_to_xmlschema }}</lastmod>
      {% endif %}

      {% if category.sitemap.changefreq == null %}
        <changefreq>weekly</changefreq>
      {% else %}
        <changefreq>{{ category.sitemap.changefreq }}</changefreq>
      {% endif %}

      {% if category.sitemap.priority == null %}
          <priority>0.5</priority>
      {% else %}
        <priority>{{ category.sitemap.priority }}</priority>
      {% endif %}
    </url>
  {% endfor %}
</urlset>